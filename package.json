{
  "name": "nafsi",
  "private": true,
  "version": "0.0.1",
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "apps/*",
    "backend"
  ],
  "scripts": {
    "dev": "turbo dev",
    "deploy": "bun --filter backend deploy",
    "mobile": "bun --filter mobile start",
    "mobile:android": "bun --filter mobile android",
    "mobile:ios": "bun --filter mobile ios",
    "mobile:web": "bun --filter mobile web",
    "backend": "bun --filter backend dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
